{
  "name": "@notification-hub/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply notification-hub --local"
  },
  "dependencies": {
    "@notification-hub/shared": "workspace:*",
    "hono": "^4.0.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/web-push": "^3.6.4",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
